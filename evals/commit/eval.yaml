name: "commit-eval"
description: "Evaluates commit subagent routing"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(commit|subagent)"

  - type: behavior
    name: response_budget
    config:
      max_tokens: 800

tasks:
  - "tasks/*.yaml"
