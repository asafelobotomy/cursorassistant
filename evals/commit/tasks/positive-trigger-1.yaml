id: positive-trigger-1
description: "Verify commit subagent is suggested for its domain"
prompt: |
  Stage these changes, write a commit message, and open a PR. Which subagent should handle this in a cursorAssistant workspace?
expected:
  - "commit"
tags:
  - smoke
