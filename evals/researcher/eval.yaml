name: "researcher-eval"
description: "Evaluates researcher subagent routing"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(researcher|subagent)"

tasks:
  - "tasks/*.yaml"
