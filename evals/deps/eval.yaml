name: "deps-eval"
description: "Evaluates deps subagent routing"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(deps|subagent)"

  - type: behavior
    name: response_budget
    config:
      max_tokens: 800

tasks:
  - "tasks/*.yaml"
