name: "docs-eval"
description: "Evaluates docs subagent routing"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(docs|subagent)"

  - type: behavior
    name: response_budget
    config:
      max_tokens: 800

tasks:
  - "tasks/*.yaml"
