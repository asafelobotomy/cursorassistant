name: "planner-eval"
description: "Evaluates planner subagent routing"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(planner|subagent)"

tasks:
  - "tasks/*.yaml"
