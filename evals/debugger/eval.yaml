name: "debugger-eval"
description: "Evaluates debugger subagent routing"

graders:
  - type: text
    name: references_agent
    config:
      pattern: "(?i)(debugger|subagent)"

tasks:
  - "tasks/*.yaml"
