# Obsolete MCP manifests

Do not use in new installs. Layered manifests live in the parent directory:

- `mcp-core.json`
- `mcp-extensions.json`
- `mcp-packs/*.json`

These files are kept for historical reference only.
